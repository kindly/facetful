[package]
name = "rx-mini"
version = "0.1.0"
edition = "2021"
[lib]
crate-type = ["cdylib"]
[dependencies]
regex = { version = "1", default-features = false, features = ["std", "perf"] }
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
[workspace]
