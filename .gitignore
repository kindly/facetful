# build artifacts
target/
target-simd/
dist/
*.wasm.gz
js/facetful/facetful_wasm.wasm

# dependencies
node_modules/

# data: real-dataset derivatives are rebuilt from the published xlsx, not tracked
# (the synthetic spike datasets under spikes/facet-spike stay tracked — history has them)
/data/
/spikes/native-bench/work/
*.xlsx

# tool state
/.sideview/
docs/*.sv.lock
bench/results.tsv
bench/baseline.tsv

# never part of the project: home-directory names that a sandboxed `git add -A`
# once picked up as empty files (the sandbox masks these paths as /dev/null)
/.bash_profile
/.bashrc
/.claude/
/.gitconfig
/.gitmodules
/.idea
/.mcp.json
/.profile
/.ripgreprc
/.vscode
/.zprofile
/.zshrc
